---
title: Installation
description: Install the Meridian TypeScript SDK.
---

```bash
npm install meridian-sdk
# or
pnpm add meridian-sdk
# or
bun add meridian-sdk
```

## Requirements

- TypeScript 5.x with `strict: true`
- Node 18+, Bun 1+, or any modern browser
- A running Meridian server (see [Docker](/self-hosting/docker))

## Examples

**Collaborative workspace** — ORSet tasks, PNCounter votes, LWWRegister title, Presence (vanilla TypeScript):

```bash
cd examples/collaborative-workspace
bun install && bun dev
```

**Live cursors** — real-time cursor tracking with `usePresence` (React):

```bash
cd examples/cursors
bun install && bun dev
```

**Collaborative AI agents** — multiple Claude agents summarize a document in parallel, a coordinator aggregates the results. Live dashboard via `useCRDTMap` + `usePresence` (React + Node.js):

```bash
cd examples/ai-agents
bun install

# Terminal 1 — UI
cd ui && bun run dev

# Terminal 2 — agents
MERIDIAN_ADMIN_TOKEN=xxx ANTHROPIC_API_KEY=xxx bun run agents
```

See [`examples/ai-agents/README.md`](https://github.com/Chahine-tech/meridian/tree/main/examples/ai-agents) for full setup instructions.
